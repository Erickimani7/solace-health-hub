dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^4.0.0
  google_maps_flutter: ^2.5.0
  http: ^1.0.0
  provider: ^6.0.0
  flutter_localizations:
    sdk: flutter
